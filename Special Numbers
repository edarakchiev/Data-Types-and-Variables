n = int(input())

for num in range(1, n + 1):
    sum_of_digits = 0
    num_str = str(num)
    for n in num_str:
        sum_of_digits += int(n)
    if sum_of_digits == 5 or sum_of_digits == 7 or sum_of_digits == 11:
        print(f"{num} -> True")
    else:
        print(f"{num} -> False")
